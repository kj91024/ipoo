#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#define MAXSIG 100

int getsigno(int signo[]);

int main(){
	int type;		
	int signo[MAXSIG]; 	/*Lista de signos*/
	while((type = getsigno(signo)) != EOF){
		
	}
}
int getsigno(int signo[]){
	int i,c;
	i = 0;
    	while((c = getchar()) == ' ' || c =='\t')
      		;

	/*if(c == '-' && c == '+')*/	
	printf("%c\n",c);
	
}
